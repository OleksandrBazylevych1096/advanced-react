import {
    defaultAddressHandlers,
    deleteAddressHandlers,
    geocodeHandlers,
    listAddressHandlers,
    searchHandlers,
    setDefaultAddressHandlers,
    shippingAddressHandlers,
} from "./api/test/handlers";
import {mockAddresses, mockSingleAddress} from "./api/test/mockData";

export {
    defaultAddressHandlers,
    deleteAddressHandlers,
    geocodeHandlers,
    listAddressHandlers,
    mockAddresses,
    mockSingleAddress,
    searchHandlers,
    setDefaultAddressHandlers,
    shippingAddressHandlers,
};
